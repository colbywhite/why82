<!DOCTYPE html>
<html lang="en">
<head>
    <title>Why82?</title>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= csrf_meta_tags %>
    <base href="<%= request.fullpath %>"/>
</head>
<body>

<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#collapsable" aria-expanded="false">
                <!-- This creates a hamburger button. -->
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <%= brand_html %>
        </div>
        <div class="navbar-collapse collapse" id="collapsable">
            <p class="navbar-text navbar-right">
                v<%= Configuration::VERSION %>
            </p>
        </div>
    </div>
</nav>

<%= yield %>

</body>
</html>
